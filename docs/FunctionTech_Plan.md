# 🤖 AI Chatbot 기능 확장 가이드: 꼬리질문 & 피드백 시스템

이 문서는 AI 챗봇의 사용자 경험을 향상시키는 **'꼬리질문(추천 질문)'** 기능과 지속적인 성능 개선을 위한 **'사용자 피드백(RLHF Lite)'** 기능의 구현 방법, 작동 원리, 그리고 문제 해결 경험을 공유하기 위해 작성되었습니다.

---

## 📚 목차
1. [기능 개요](#1-기능-개요)
2. [Part 1: 꼬리질문 (Smart Follow-up Suggestions)](#part-1-꼬리질문-smart-follow-up-suggestions)
3. [Part 2: 피드백 & 셀프 러닝 (RLHF Lite)](#part-2-피드백--셀프-러닝-rlhf-lite)
4. [구현 시 주의 사항 & 트러블슈팅](#4-구현-시-주의-사항--트러블슈팅)

---

## 1. 🔍 기능 개요

### A. 꼬리질문 (Smart Suggestions)
사용자의 질문에 답한 후, **"다음에 물어볼 법한 질문"**을 AI가 자동으로 생성하여 버튼 형태로 제공합니다.
- **예시**: 사용자가 "컴퓨터공학과 위치가 어디야?"라고 물으면, -> **[등록금은 얼마인가요?] [입학 조건은?] [교수님 정보]** 등을 추천.

### B. 피드백 시스템 (Reinforcement Learning from Human Feedback - Lite)
사용자가 답변에 👍(좋아요) / 👎(싫어요)를 누르면 이를 저장하고, **다음 답변 생성 시 참고**합니다.
- **예시**: "A학과 등록금"에 대한 답변이 부정확해서 👎를 받았다면, 다음번 유사 질문 시 이 답변을 피하도록 **GPT 프롬프트에 '나쁜 예시'로 주입**합니다.

---

## Part 1: 꼬리질문 (Smart Follow-up Suggestions)

### 🛠 작동 원리
1. 사용자가 질문을 던지고, AI가 답변을 생성합니다 (1차 LLM 호출).
2. **질문 + 답변**을 다시 LLM에 보내어 *"이 상황에서 사용자가 궁금해할 질문 3가지를 만들어줘"*라고 요청합니다 (2차 LLM 호출).
3. 생성된 질문 리스트를 프론트엔드에 전달하여 클릭 가능한 버튼으로 표시합니다.

### 💻 핵심 구현 코드

#### 1. AI Engine (`ai_engine.py`)
응답 생성 후 후속 질문을 만드는 메서드를 추가합니다.

```python
def generate_suggestions(self, user_message: str, ai_response: str) -> list:
    """
    대화 맥락을 기반으로 2~3개의 후속 질문 생성
    """
    suggestion_prompt = f"""Based on this conversation, generate 2-3 natural follow-up questions.
    
    User's Question: {user_message}
    AI's Response: {ai_response[:500]}
    
    Respond ONLY with a JSON array of questions:
    ["Question 1?", "Question 2?", "Question 3?"]"""

    try:
        # LLM 호출 (JSON 모드 사용 권장)
        payload = {
            "model": self.model_name,
            "messages": [{"role": "user", "content": suggestion_prompt}],
            "format": "json"    # 중요: JSON 포맷 강제
        }
        # ... (request 요청 코드)
        
        # 파싱 로직 (매우 중요 - 트러블슈팅 참조)
        content = response.json().get("message", {}).get("content", "[]")
        parsed = json.loads(content)
        
        # Case 1: 리스트가 바로 반환된 경우 ["Q1", "Q2"]
        if isinstance(parsed, list): return parsed[:3]
        
        # Case 2: 객체로 감싸진 경우 {"questions": ["Q1", "Q2"]}
        if isinstance(parsed, dict):
             for key, value in parsed.items():
                 if isinstance(value, list): return value[:3]
             # Case 3: 키 자체가 질문인 경우 {"Q1": [], "Q2": []}
             return list(parsed.keys())[:3]
             
    except Exception as e:
        return []
```

#### 2. Backend Integration Logic (Server-agnostic)
이 로직은 Flask, FastAPI, Django 등 어떤 프레임워크를 사용하든 **채팅 API의 응답 반환 직전**에 삽입하면 됩니다.

**필요한 로직 흐름**:
1. AI 모델로부터 기본 답변(`response`)을 받는다.
2. `generate_suggestions(user_message, response)` 함수를 호출한다.
3. 클라이언트에게 보내는 JSON 응답에 `suggestions` 필드를 추가한다.

**코드 예시 (Python Generic)**:
```python
# 기존 채팅 로직
ai_response = ask_llm(user_message) 

# ✅ [추가할 코드] 꼬리질문 생성
# 별도 모듈로 분리하거나 유틸리티 함수로 선언하여 사용 권장
suggestions = generate_suggestions(user_message, ai_response)

# 최종 반환 데이터 구성
return_data = {
    "response": ai_response,
    "suggestions": suggestions  # <--- 이 필드를 추가
}
```

---

## Part 2: 피드백 & 셀프 러닝 (RLHF Lite)

### 🛠 작동 원리
1. **저장**: 사용자가 좋아요/싫어요를 누르면 질문, 답변, 점수(1/-1)를 DB에 저장합니다.
2. **검색**: 다음 질문이 들어오면, DB에서 **유사한 과거 질문**을 검색합니다.
3. **주입**: 검색된 '좋은 답변'과 '나쁜 답변'을 **System Prompt**에 참고 자료(Context)로 넣어줍니다.

### 💻 핵심 구현 코드 (Modular)

#### 1. Feedback Manager Component
피드백 저장 및 검색을 담당하는 독립적인 클래스나 함수로 구현하는 것을 권장합니다.

```python
class FeedbackManager:
    def __init__(self, db_client):
        self.db = db_client

    def save_feedback(self, query, response, score):
        # 1. 개인정보(PII) 필터링 (필수)
        if has_pii(response): return False 
        
        # 2. DB 저장
        self.db.insert({
            "query": query, "response": response, "score": score, "timestamp": now()
        })

    def get_related_examples(self, current_query):
        # 1. 유사도 검색 (키워드 매칭, 벡터 검색 등)
        # ... 로직 구현 ...
        return {"good": [...], "bad": [...]}
```

#### 2. Context Injection Logic
AI에게 질문을 던질 때, 프롬프트를 동적으로 구성하는 부분입니다.

```python
# 프롬프트 구성 예시
system_prompt = "You are a helpful assistant."

# ✅ [추가할 코드] 피드백 반영
examples = feedback_manager.get_related_examples(user_input)

if examples['good']:
    system_prompt += f"\nReference these good answers: {examples['good']}"
if examples['bad']:
    system_prompt += f"\nAvoid these mistakes: {examples['bad']}"

# 최종 LLM 호출
llm.chat(system_prompt, user_input)
```

---

## 4. 🧩 통합 가이드 (Integration Strategy)

팀원의 프로젝트에 이 기능을 적용할 때는 다음 단계를 따르세요. 기존 파일 구조를 깨지 않고 "조각(Snippet)"을 끼워 넣는 방식입니다.

### Step 1: AI Engine 확장
팀원의 프로젝트에 LLM을 호출하는 함수나 클래스가 있을 것입니다 (예: `call_gpt()`, `ChatBot.ask()` 등).
-> 해당 파일에 `generate_suggestions` 함수를 **새로운 메서드**로 추가하세요.

### Step 2: API 응답 수정
클라이언트(프론트엔드)와 통신하는 API 엔드포인트(예: `views.py` 또는 `routes.py`)를 찾으세요.
-> `return` 문 직전에 `generate_suggestions`를 호출하고, 반환되는 JSON에 `suggestions` 키를 추가하세요.

### Step 3: 프론트엔드 버튼 추가
AI의 응답을 화면에 뿌려주는 자바스크립트 함수(예: `appendMessage()`)를 찾으세요.
-> 응답 메시지 출력 후, `suggestions` 데이터가 있다면 버튼을 동적으로 생성하는 UI 코드를 추가하세요.

---

## 5. 🚨 구현 시 주의 사항 & 트러블슈팅

### 1) LLM JSON 파싱 지옥 (가장 많이 겪는 문제)
LLM에게 "JSON 배열로 줘"라고 해도 다양한 형태로 줍니다. 파싱 로직을 유연하게 짜야 합니다.
- **요청**: `["Q1", "Q2"]`
- **실제 응답 유형들**:
  1. `["Q1", "Q2"]` (정상)
  2. `{"questions": ["Q1", "Q2"]}` (객체로 감쌈)
  3. `{"0": "Q1", "1": "Q2"}` (숫자가 키 - **현재 겪은 오류**)
  4. `{"Q1": [], "Q2": []}` (질문을 키로 사용)

**해결책**:
- 리스트인지 확인(`isinstance(parsed, list)`)
- 딕셔너리라면 `values()` 중에 문자열 리스트가 있는지 확인.
- 없다면 `keys()` 중에 질문이 있는지 확인하는 등 **다중 방어 로직**이 필요합니다.
- **팁**: 수집된 문자열이 **물음표(`?`)로 끝나는지 확인**하면, "생각(Thought)"이나 "메타데이터"가 버튼으로 노출되는 사고를 막을 수 있습니다.

### 2) 로컬 LLM 타임아웃
꼬리질문 생성은 2번째 LLM 호출이므로 전체 응답 시간이 길어집니다.
- **증상**: `Read timed out` 오류 발생.
- **해결책**: 타임아웃을 넉넉하게(최소 30초) 설정하세요.

### 3) 프라이버시 이슈 (피드백 저장 시)
사용자가 "내 학번은 12345야"라고 말한 내용이 DB에 '좋은 예시'로 저장되면 안 됩니다.
- **해결책**: `save_feedback` 메서드에 정규식 필터를 적용하여 **숫자(학번, 전화번호)나 민감 키워드가 포함된 경우 저장을 거부**하세요.

### 4) 품질 관리 (Quality Control - 중요)
AI가 "정보가 없습니다", "로그인이 필요합니다"라고 답했는데, 그 밑에 [학점은?] [전공은?] 같은 질문을 추천하면 사용자 경험이 나빠집니다.
- **해결책**: API 단에서 AI 응답 텍스트에 부정적인 키워드("don't have access", "not found")가 포함되어 있다면 `generate_suggestions` 호출을 아예 생략(`skip`)하도록 처리하세요.

---

## 6. 🚀 부록: Gemini API로 업그레이드하기(이미 Gemini API를 사용하고 있다면 무시하세요.)
현재 Local LLM(Ollama) 방식은 무료지만 속도가 느립니다. 추후 **Gemini API**로 교체하면 속도가 획기적으로 빨라지며, 코드는 **`ai_engine.py` 딱 하나만 수정**하면 됩니다. (나머지 코드는 그대로 사용 가능)

### 변경 가이드 (`ai_engine.py`)

```python
# 1. 라이브러리 설치
# pip install google-generativeai

import google.generativeai as genai

class AIEngine:
    def __init__(self):
        # API 키 설정
        genai.configure(api_key="YOUR_API_KEY")
        self.model = genai.GenerativeModel('gemini-pro')

    def get_response(self, user_message, ...):
        # 기존 requests.post 코드를 아래 2줄로 대체
        response = self.model.generate_content(prompt)
        return response.text

    def generate_suggestions(self, user_message, ai_response):
        prompt = "..." # 프롬프트 동일
        
        # JSON 모드 사용 (Gemini 1.5 Pro 이상 권장)
        response = self.model.generate_content(
            prompt, 
            generation_config={"response_mime_type": "application/json"}
        )
        # 이후 파싱 로직은 동일하게 사용
```

### ✅ 결론
이 두 기능을 적용하면 챗봇이 훨씬 "똑똑해" 보이며, 사용할수록 데이터가 쌓여 성능이 좋아지는 선순환 구조를 만들 수 있습니다. 구현 난이도 대비 효과가 매우 큽니다.